# 功能方向规划与技术债重构 (TODO)

经过近期的项目全面检阅与中二病词汇清理，我们确定了部分深水区代码的技术债，并规划了未来的功能优化方向。

## 1. 结构重构计划 (Refactoring)
虽然项目的模块化已相当成熟，但仍有以下高优重构点：

- **~~巨型组件组件化 (`AppSidebar.vue`)~~** ✅ 已完成
  已将 `AppSidebar.vue`（866L → 324L）拆分为三个独立子组件：
  - `SidebarFolderTree.vue` — 文件夹树递归逻辑 + 右键菜单
  - `SidebarFooter.vue` — 底部区域（设置/微件/导入导出/版本号）
  - `SidebarShortcutPanel.vue` — 快捷键面板

- **~~桌面悬浮微件页面下沉隔离 (`views/popout/`)~~** ✅ 已完成
  已将 5 个非路由子组件（`HubExpandedBody`, `HubTasks`, `HubTimer`, `HubLife`, `HubSettings`）从 `views/popout/` 迁移至 `components/popout/`，`views/popout/` 现仅保留 3 个路由页面。

## 2. 交互体验打磨 (UX/UI Polish)
- **设置页数据交互体验**：强化用户对数据导出、存储位置迁移操作的防呆提示进度及平滑化。
- **拖放与右键能力延展**：现有的侧边栏可尝试扩展拖拽合并笔记分类的逻辑。

## 3. 面向桌面原生能力的扩展
- 后续探索引入开机自启等 Tauri 深度集成逻辑的进一步完善。
- 增强时间条跨平台窗口圆角阴影。
