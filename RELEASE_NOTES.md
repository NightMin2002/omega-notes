## 更新日志 - 2026-05-25

### style/feat: 真实呈现自动更新下载进度并在下载完成后提供2.5秒重启缓冲
1. 自动更新逻辑调优：重构 updater.ts，取消 99% 的视觉限制，顺其自然地反映真实的下载进度推进；并在 Finished 触发、即包下载安装完毕后，通过 Promise 注入平滑的 2.5 秒黄金视觉与操作缓冲期，然后再执行 relaunch()。
2. 状态文字深度适配：SettingsView.vue 状态文字优化，当下载进度跑满 100% 后，自适应切换展示“下载完成，正在准备重启更新...”，同时隐退无意义的字节率计算，为用户提供极其清晰、温馨的重启状态引导。
3. 交互摩擦根治：完美克服在超高速宽带下，高频下载事件在 Vue 渲染队列积压，导致来不及看清进度条拉满、程序便强启闪退的体验痛点。

### style/feat: 新增全局多主题切换并优化窗口唤醒与托盘交互体验
1. 样式与状态层：全局 variables.css 新增“暖墨灰”及“羊皮纸”两大低对比度护眼主题，扩展 theme.ts 状态管理至四元主题，支持顺序循环轮换与直接调用接口。
2. 组件与页面适配：AppHeader.vue 适配四主题专属 SVG 图标与响应式 tooltip 展示；SettingsView.vue 扩展为四元 switcher 并重构为 CSS Grid 网格，支持手机窄屏下 2x2 自适应。
3. 窗口定位对齐：tauri.conf.json 窗口配置 center: true 确保冷启动居中；lib.rs 在单实例唤醒、托盘右键显示及左键点击托盘时皆调用 window.center() 动作，保障全场景窗口无死角对齐。
4. 托盘交互打磨：lib.rs 托盘构建链追加 show_menu_on_left_click(false)，彻底根治 Windows 平台下左键单击图标时闪烁并误触发右键上下文菜单的顽疾 bug。
5. 项目文档同步：STRUCTURE.md 同步更新 variables.css 与 theme.ts 职责表。
